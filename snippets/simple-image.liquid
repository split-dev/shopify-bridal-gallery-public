{% comment %}
Inputs:
- image (object) - image to render;
- imageLoading (string: 'lazy'/'');
- imageClasses(string) - classes for rendered image;
{%endcomment%}

{%- liquid
    assign imageWidth = image.width
    assign imageHeight = imageWidth | divided_by: image.aspect_ratio
%}

{{ image | image_url: width: 3840 | image_tag:
loading: imageLoading,
width: imageWidth,
height: imageHeight,
class: imageClasses,
sizes: '100vw',
widths: '375, 550, 750, 1100, 1500, 1780, 2000, 3000, 3840' }}